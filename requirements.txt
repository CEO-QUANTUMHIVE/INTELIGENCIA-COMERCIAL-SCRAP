fastapi==0.118.0
uvicorn[standard]==0.37.0
pydantic==2.11.9
python-dotenv==1.1.1
httpx==0.28.1

# Scraping
# Scrapling exige playwright>=1.61: lo dejamos que él fije la versión.
scrapling[fetchers]==0.4.14
playwright>=1.61.0
# Patchright: Playwright parcheado anti-detección. google_maps.py lo usa
# directo (Scrapling ya lo trae para Fetcher/StealthyFetcher, pero Maps
# necesita el navegador real con sesión, no un fetch puntual).
patchright>=1.61.0

# Colores dominantes del logo (Módulo 1: identidad visual para Fábrica de Webs)
Pillow==12.3.0

# Base de datos
# Tiene que ir a la par de postgrest/storage3/realtime/supabase-auth: la 2.20
# rompe con el supabase-auth 2.31 que arrastra pip.
supabase==2.31.0
# Conexión directa a Postgres: aplica el esquema sin pasar por el SQL Editor
psycopg[binary]==3.3.4

# IA (los dos, se elige por variable de entorno PROVEEDOR_IA)
anthropic==0.75.0
openai==2.6.1

# Pruebas
pytest==8.4.2
